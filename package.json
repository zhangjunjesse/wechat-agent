{
  "name": "wechat-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Multi-tenant WeChat Bot Agent service skeleton",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "start": "node src/server.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "qrcode": "^1.5.4",
    "@openai/agents": "^0.2.3"
  }
}
